---
description: Sample Commands
commands:
   - name: echo 
     description: Echoes the message.
     type: Commandir.Builtins.Console
     parameters:
        message: "Hello World!"
        prefix: "Default: "
     arguments:
        - name: message
          description: The messsage.
     options:
        - name: prefix
          description: The prefix (optional).
          required: false
       